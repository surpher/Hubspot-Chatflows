# Hubspot Chat SDK for iOS

1. Configure `HubspotManager` in [`HubspotManager_ChatflowsApp.swift`](Hubspot-Chatflows/Hubspot_ChatflowsApp.swift) by setting `portalId`.
2. Make sure there are at least two distinct chatflows configured for the given `portalId`.
3. Run the app.
